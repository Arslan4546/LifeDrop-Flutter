name: life_drop
description: "Blood donation management app with OneSignal notifications & Supabase backend"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # ============================================
  # 🧠 STATE MANAGEMENT & ROUTING
  # ============================================
  flutter_bloc: ^9.1.1              # BLoC state management
  go_router: ^17.1.0                # Declarative routing system
  get_it: ^9.2.1                    # Service locator / Dependency Injection
  injectable: ^2.4.0                # Code generation for DI registration
  equatable: ^2.0.5                 # Value comparison for bloc states
  rxdart: ^0.28.0                   # Reactive streams for BLoC
  
  # ============================================
  # 🔥 BACKEND - SUPABASE
  # ============================================
  supabase_flutter: ^2.3.4          # Official Supabase SDK (Auth, DB, Storage, Realtime)
  
  # ============================================
  # 🔔 PUSH NOTIFICATIONS - ONESIGNAL
  # ============================================
  onesignal_flutter: ^5.1.2         # Push notifications service (Free tier: 10k/month)
  
  # ============================================
  # 🗺️ MAPS & LOCATION
  # ============================================
  google_maps_flutter: ^2.5.0       # Interactive map widget
  geolocator: ^14.0.2               # GPS position & permission handling
  geocoding: ^4.0.0                 # Reverse geocoding (coordinates → address)
  location: ^8.0.1                  # Advanced location tracking & permissions
  
  # ============================================
  # 📊 DATA HANDLING & JSON
  # ============================================
  json_annotation: ^4.9.0           # JSON serialization annotations
  freezed_annotation: ^3.1.0        # Immutable models with freezed
  
  # ============================================
  # 🎨 UI COMPONENTS
  # ============================================
  cupertino_icons: ^1.0.8           # iOS style icons
  cached_network_image: ^3.3.1      # Network image caching
  shimmer: ^3.0.0                   # Loading skeletons
  flutter_svg: ^2.0.9               # Vector icon support
  intl: ^0.20.2                     # Date/time formatting utilities
  percent_indicator: ^4.2.3         # Custom progress indicators
  auto_size_text: ^3.0.0            # Responsive text sizing
  
  # ============================================
  # 🔐 AUTHENTICATION
  # ============================================
  sign_in_with_apple: ^7.0.1        # Apple Sign-in (required for iOS)
  url_launcher: ^6.2.4              # External links / phone calls
  
  # ============================================
  # 💾 STORAGE & UTILITIES
  # ============================================
  shared_preferences: ^2.2.2        # Local key-value storage
  path_provider: ^2.1.2             # File paths for local storage
  uuid: ^4.4.0                      # Unique ID generation
  flutter_dotenv: ^6.0.0            # Environment variable management (.env files)
  
  # ============================================
  # 🔕 LOCAL NOTIFICATIONS (Optional)
  # ============================================
  flutter_local_notifications: ^21.0.0  # Scheduled/local notifications

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for deactivating specific lint rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code Generation Tools (Must be run after each dependency change)
  build_runner: ^2.4.8              # Run code generators
  injectable_generator: ^2.4.1      # Generate DI injection files
  json_serializable: ^6.8.0         # JSON serialization codegen
  freezed: ^3.2.5                   # Freezed models codegen
  
  # Testing Helpers
  bloc_test: ^10.0.0                 # Unit test helpers for BLoCs
  mockito: ^5.4.4                   # Mocking for unit tests
  golden_toolkit: ^0.15.0           # Screenshot testing for UI

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # ============================================
  # ASSETS SECTION (Required for .env & images)
  # ============================================
  assets:
    - .env                          # Environment variables (Supabase, OneSignal, Maps Keys)
    - assets/images/
    - assets/icons/
    - assets/fonts/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/font-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package